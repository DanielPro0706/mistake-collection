interface:
  display_name: "Mistake Collection"
  short_description: "Manually rebuild and fully verify mistake PDFs."
  default_prompt: "Use $mistake-collection to inspect source photos manually, solve independently, web-cross-check answers without blindly trusting them, try ImageGen first for every feasible figure, and fully verify both PDFs before delivery."
