interface:
  display_name: "Mistake Collection"
  short_description: "Manually rebuild and fully verify mistake PDFs."
  default_prompt: "Use $mistake-collection to inspect source photos manually, try ImageGen first for every feasible figure, and fully verify the ordered question-only and answer PDFs before delivery."
