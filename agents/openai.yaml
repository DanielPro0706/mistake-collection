interface:
  display_name: "Mistake Collection"
  short_description: "Create a strict questions-only PDF plus a detailed solution PDF."
  default_prompt: "Use $mistake-collection to create a strict question-only PDF with no knowledge points or hints, plus a synchronized detailed red-solution PDF from these source images."
