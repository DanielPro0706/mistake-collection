interface:
  display_name: "Mistake Collection"
  short_description: "Manually rebuild and fully verify mistake PDFs."
  default_prompt: "Use $mistake-collection to organize the uploaded problem photos in the current workspace. Infer the chapter title unless I explicitly provide one, and apply all built-in reconstruction, solution, formatting, and verification rules."
