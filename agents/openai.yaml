interface:
  display_name: "Mistake Collection"
  short_description: "Manually rebuild photos into two verified mistake PDFs."
  default_prompt: "Use $mistake-collection to manually inspect these source images, redraw feasible figures with ImageGen, and create synchronized question-only and red-solution PDFs with aligned step commentary."
